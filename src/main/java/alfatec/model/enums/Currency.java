package alfatec.model.enums;

/**
 * Model for database enum in column "currency" in "registration_fee" table.
 * 
 * As requested, has only 3 values: RSD, EUR, USD.
 * 
 * @author jelena
 *
 */
public enum Currency {
	RSD, EUR, USD
}
